import styled from 'styled-components';

export const LayoutNews = styled.div`
    display: grid;
    background: #333333;
    background-size: cover;
    height: 100vh;
    overflow: auto;
    grid-template-columns: repeat(auto-fill, minmax(auto, 407.3px));
    grid-auto-rows: min-content;
`;
